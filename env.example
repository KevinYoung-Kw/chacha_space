# ==================== 叉叉 AI 助手 环境变量配置 ====================
# 
# 【重要】此文件放置位置：项目根目录（与 docker-compose.yml 同级）
#
# 使用说明：
#   1. 复制此文件为 .env：cp env.example .env
#   2. 填入真实的 API 密钥
#   3. 开发环境：backend/src/config.ts 会从项目根目录读取
#   4. Docker 环境：docker-compose.yml 会自动注入环境变量
#
# ==================== 必需配置 ====================

# MiniMax API 配置（语音合成、AI对话）
# 获取地址: https://platform.minimaxi.com/
MINIMAX_API_KEY=your-minimax-api-key-here
MINIMAX_GROUP_ID=your-minimax-group-id-here

# ==================== 可选配置 ====================

# 高德地图 API（天气服务，可选）
# 获取地址: https://console.amap.com/
AMAP_KEY=your-amap-api-key-here

# 阶跃星辰 API（情绪检测，可选）
# 获取地址: https://platform.stepfun.com/
STEPFUN_API_KEY=your-stepfun-api-key-here

# ==================== 服务器配置 ====================

# 运行环境
NODE_ENV=production

# 服务端口
PORT=7860

# JWT 密钥（生产环境必须修改为随机字符串！）
# 生成方法: openssl rand -hex 32
JWT_SECRET=chacha-secret-PLEASE-CHANGE-THIS-IN-PRODUCTION

# 数据库路径
DATABASE_PATH=/app/data/chacha.db

# 静态文件路径（Docker 部署时使用）
STATIC_PATH=/app/backend/frontend/dist

# CORS 配置
# 开发环境: *（允许所有来源）
# 生产环境: 建议改为具体域名，如 https://yourdomain.com
CORS_ORIGIN=*

# ==================== 配置验证 ====================
# 
# 启动后会自动检查以下配置：
#   ✓ 必需：MINIMAX_API_KEY, MINIMAX_GROUP_ID
#   ⚠️ 推荐：自定义 JWT_SECRET（生产环境）
#   - 可选：AMAP_KEY, STEPFUN_API_KEY
#
# 查看配置状态：
#   开发环境: npm run dev
#   Docker: docker-compose logs chacha
#
